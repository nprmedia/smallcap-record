This repo is a public, pre-registered stock research record. METHODOLOGY.md is the pre-registration — never edit it except via its §9 protocol (config bump + memo + strikethrough, never deletion). config/v0.yaml is the source of truth for all parameters; code reads from it, never hardcodes. Conventions: run outputs go in runs/YYYY-MM-DD/; full_*.csv stays local (gitignored), public_*.csv gets committed; every run: ots stamp new artifacts and ots upgrade prior receipts, commit receipts. Schema is append-only — new columns allowed, existing ones never redefined. Never commit .env, API keys, or full_*.csv. The user is finance-fluent but not a coder: explain in plain language, you do the typing.
